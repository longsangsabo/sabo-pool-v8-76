import React from 'react';
import TournamentCard from '@/components/TournamentCard';

// Optimized Tournament Card - extends the base TournamentCard
export const OptimizedTournamentCard = React.memo(TournamentCard);

export default OptimizedTournamentCard;
